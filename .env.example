# Docker Compose Configuration
# Copy this file to .env and adjust values as needed

# GitHub Container Registry (for docker-compose.prod.yml)
GITHUB_REPOSITORY=your-username/agent-executor
IMAGE_TAG=latest

# Control Server Configuration
# CONTROL_SERVER_PORT: The port on the HOST machine (not the container port)
# The control-server always listens on port 3000 INSIDE the container
# This variable maps that to a different port on your host machine
CONTROL_SERVER_PORT=7001
CONTROL_SERVER_CONTAINER_NAME=control-server

# Agent Configuration
AGENT_CONTAINER_NAME=agent

# IMPORTANT: SERVER_URL must use the INTERNAL container port (3000)
# NOT the external host port (CONTROL_SERVER_PORT)
# Containers communicate directly via Docker network, not through host ports
SERVER_URL=http://control-server:3000

POLL_INTERVAL=2000
AGENT_ID=

# Environment
NODE_ENV=production

# Healthcheck Configuration
HEALTHCHECK_INTERVAL=10s
HEALTHCHECK_TIMEOUT=5s
HEALTHCHECK_RETRIES=3
HEALTHCHECK_START_PERIOD=10s
